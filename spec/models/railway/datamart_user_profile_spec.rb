require 'spec_helper'

describe DatamartUserProfile do
  # let(:branch_employee) { build_stubbed(:branch_employee) }

  it 'should have an branch assigned' #do
  #   branch_employee.profile_for_branch(branch_employee.branches.last.id).try(:datamart_user_profile).should_not be_nil
  # end
end
