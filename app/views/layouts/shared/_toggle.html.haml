-#= link_to(( object.send("is_#{field}?") ? "<span class='toggle_on'>&nbsp;</span>" : "<span class='toggle_off'>&nbsp;</span>"), :url => { :controller => object.class.to_s.pluralize.downcase, :action => "toggle_confirmation",  :field => field, :id => object, :klass => object.class }, :remote => true, :update => "record_#{object.id}_#{field}" )

- toggle_status = object.send("is_#{field}?") ? "<span class='toggle_on'>&nbsp;</span>" : "<span class='toggle_off'>&nbsp;</span>"

<span class='toggle' id="record_#{object.id}_#{field}"> 
= link_to(toggle_status)#, :url => { :controller => object.class.to_s.pluralize.downcase, :action => "toggle_confirmation",  :field => field, :id => object, :klass => object.class }, :remote => true, :update => "record_#{object.id}_#{field}" )
</span>